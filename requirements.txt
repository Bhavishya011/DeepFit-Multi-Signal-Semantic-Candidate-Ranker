# ─── Core ranking dependencies (required for rank.py) ─────────────────────
# These are the ONLY deps needed to run `python rank.py` and produce submission.csv

# Retrieval
rank-bm25==0.2.2
faiss-cpu==1.8.0

# Data + math
numpy>=1.26.0,<3.0
pandas>=2.0.0
scipy>=1.11.0
scikit-learn>=1.3.0

# Config + I/O
pyyaml>=6.0
pyarrow>=14.0.0
tqdm>=4.65.0

# ─── Optional: Pre-compute BGE-small embeddings (higher quality) ──────────
# Uncomment these if you want to pre-compute BGE-small embeddings instead of
# using the TF-IDF fallback. The pipeline works without these — it auto-falls
# back to in-memory TF-IDF embeddings if sentence-transformers isn't installed.
#
# sentence-transformers>=3.0.0
# torch>=2.0.0  # install CPU version: pip install torch --index-url https://download.pytorch.org/whl/cpu
# transformers>=4.40.0

# ─── Dev / testing only (not required for submission) ─────────────────────
# pytest>=8.0.0
# pytest-timeout>=2.3.0
