# ─── Pre-computation (offline, network allowed) ───────────────────────────
sentence-transformers==3.0.1
FlagEmbedding==1.3.0
onnxruntime==1.18.0
optimum==1.21.0
torch==2.2.2 --index-url https://download.pytorch.org/whl/cpu
transformers==4.42.0
rank-bm25==0.2.2

# ─── Ranking step (constrained: 5min, 16GB, CPU, no network) ──────────────
faiss-cpu==1.8.0
numpy==1.26.4
pandas==2.2.2
scipy==1.13.0
pyarrow==16.1.0
pyyaml==6.0.1
tqdm==4.66.4

# ─── Dev / testing only ───────────────────────────────────────────────────
pytest==8.2.0
pytest-timeout==2.3.1
ruff==0.4.7

# ─── Optional: JD-Intent Decoder LLM call (one-time, offline) ─────────────
# anthropic==0.28.0  # uncomment if using Claude for JD decode
# openai==1.35.0     # uncomment if using GPT-4 for JD decode
